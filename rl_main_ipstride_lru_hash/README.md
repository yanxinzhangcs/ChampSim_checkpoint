This directory stores summary-only results produced by `python3 -m rl_controller.main`.

Each trace subdirectory contains only `episode_summary.json` on `master`.
Full artifacts remain outside GitHub.
